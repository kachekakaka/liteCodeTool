# LiteCodeTool 0.2.0 · 快速入门手册

欢迎使用 **LiteCodeTool（轻量代码工具）**！本项目是一个高颜值、科技蓝风格、极简实用的轻量化低代码大屏制作与运行平台。

本交付包采用**双轨交付设计**，无论您是只想在本地直接运行看效果，还是希望基于源码进行深度二次开发，均能极速上手。

---

## 目录索引
- [一、极速直接运行（面向演示与业务人员）](#一极速直接运行面向演示与业务人员)
- [二、核心交互与功能操作指南](#二核心交互与功能操作指南)
- [三、源码二次开发（面向研发工程师）](#三源码二次开发面向研发工程师)
- [四、交付包目录结构说明](#四交付包目录结构说明)
- [五、常见问题与故障排查](#五常见问题与故障排查)

---

## 一、极速直接运行（面向演示与业务人员）

> **运行前提**：电脑中安装了 **Node.js 20 或更新版本**（建议 [nodejs.org](https://nodejs.org) 下载 LTS 版本）。  
> **运行特色**：**完全不需要执行 `npm install` 下载依赖，不需要配置数据库，断网离线也能秒开！**

### 1. 运行步骤
1. 将本压缩包解压到一个普通的本地文件夹（如 `D:\LiteCodeTool`）；
2. **Windows 系统**：直接鼠标双击运行根目录下的 **`启动演示.cmd`**；  
   **Linux / macOS 系统**：在终端进入解压目录，运行 `sh start-demo.sh` 或 `node launcher.mjs`；
3. 稍等 1~2 秒，系统将自动在默认浏览器打开：  
   👉 **`http://127.0.0.1:8787/#viewer`**
4. 运行过程中请保持命令行黑色窗口开启；如需关闭服务，直接关闭黑框窗口或在窗口内按 `Ctrl + C` 即可。

### 2. 真实模式运行（生产数据接入）
- **Windows 系统**：双击根目录下的 **`启动真实模式.cmd`**（或执行 `node launcher.mjs --live`）；
- 默认访问端口为 `8788`，页面地址为 `http://127.0.0.1:8788/#editor`；
- 真实模式不会注入演示数据，未收到数据的字段将如实显示 `--`；启动器会在外部数据目录 `liteCodeTool_datas/connection.json` 中自动生成唯一的安全接入凭据，外部系统可通过 `POST /api/ingest` 进行数据推流。

---

## 二、核心交互与功能操作指南

### 1. 科技大屏展示态 (Viewer Mode)
- **优雅防误触的换船交互**：鼠标移入监控卡片时，仅在卡片右上角平滑浮现小巧的切换按钮（`⇄`）；**悬停不会自动弹窗遮挡**，只有主动点击 `⇄` 按钮时，才会展开切换监控对象的微型面板。
- **监控对象抽屉**：点击大屏底部控制条的【监控对象】按钮，可从屏幕右侧滑出全局管理抽屉，集中检索并批量切换大屏所有组件指派的船舶。
- **返回编辑设计态**：点击屏幕右下角的【返回编辑】按钮，即可无缝切入三栏编辑工作台。

### 2. 画布可视化编辑态 (Editor Mode)
- **拖拽排版与缩放**：从左侧资产库将组件拖入画布，点击卡片左上角拖动图标（`⠿`）移动位置，选中卡片后拖动右下角高亮把手等比自由缩放；
- **双击就地文字编辑**：
  - **大屏标题 / 卡片标题**：鼠标直接双击即可原地激活输入框直接打字，按 `Enter` 或鼠标失焦自动保存；
  - **卡片副标题**：鼠标双击卡片头部右侧的文字（如“目标监控”）即可就地修改；**若直接清空，卡头右侧文字将完全隐藏不占空间**，支持极简风格。
- **属性检查器（Inspector）常驻卡头设置**：
  - 选中任何卡片、或点击卡片内部的具体数字/图表，右侧属性检查器**最顶端始终常驻【组件卡头设置】**；
  - 包含：☑️ **显示组件卡头开关**、📝 **主标题输入框**、📝 **副标题输入框**（支持随时一键“恢复默认副标题”）；
  - 无论点中卡片哪一部分，都不会丢失卡头开关与标题修改入口。
- **多槽位组件**：单个卡片可同时容纳多个监控对象（如“本船”与“会遇船”关键指标对比），内部不同控件独立绑定不同槽位。
- **数据导入与导出**：大屏工具栏提供一键【导出】与【导入】功能，生成标准 `.litecode.json` 工程文件，便于项目迁移分享。

---

## 三、源码二次开发（面向研发工程师）

如果您希望修改平台源码、增加新原子控件或接入定制业务逻辑：

### 1. 源码目录与环境准备
源码完整保存在本包的 **`source/liteCodeTool/`** 目录中。
```bash
# 1. 打开终端，进入源码目录
cd source/liteCodeTool

# 2. 安装开发依赖（需要连接 npm 镜像）
npm install
```

### 2. 常用开发命令
| 脚本命令 | 说明 | 适用场景 |
| --- | --- | --- |
| `npm run dev:demo` | 启动开发服务器（含热更新 HMR） | 日常前端功能与组件开发（前端 5173 + 后端 8787） |
| `npm test` | 运行全量纯函数单元测试 | 验证数据解析、时效算法、契约校验（极速无外部依赖） |
| `npm run typecheck` | 执行 TypeScript 严格类型检查 | 保证全项目无类型隐患（vue-tsc 检查） |
| `npm run build` | 离线独立编译 | 将 `src/` 编译为无外部 npm 依赖的 `app/` 交付包 |
| `npm run build:vite` | 双轨 Vite 生产构建 | 构建出标准 Web 静态 Chunk |
| `npm run start` | 启动本地生产服务 | 基于编译好的 `app/` 资产启动本地服务 |

### 3. 核心架构与代码组织
- **`src/engine/core.ts`**：核心纯函数逻辑层（实体数据池 `EntityStore`、字段映射解析 `resolveValue`、滑动窗口算法、大屏契约校验等）；
- **`src/runtime.ts`**：响应式运行时状态管理、撤销重做快照（Checkpoint）、WebSocket 实时通信；
- **`src/types.ts`**：核心领域模型（`ComponentTemplate`, `ComponentInstance`, `Control`, `Slot` 等）；
- **`src/components/`**：
  - `InstanceCard.vue`：卡片容器组件（拖拽手柄、卡头双击编辑、点击换船、自适应排版）；
  - `Inspector.vue`：右侧属性检查器（置顶卡头设置、数据绑定、外观布局）；
  - `ControlRenderer.vue`：原子控件渲染器（数值、文本、指示灯、表格、图片/动效）；
  - `LineChart.vue`：SVG 矢量动态滑动时序折线图（支持对象槽位曲线与全局统计曲线）；
  - `ObjectDrawer.vue`：显示态右侧滑出式监控对象指派抽屉。
- **`server/`**：轻量级持久化与 WebSocket 实时数据分发服务。
- **`docs/adr/`**：已归档的系统架构决策记录（ADR 0001 ~ ADR 0008）。

---

## 四、交付包目录结构说明

```text
LiteCodeTool-v0.2.0/
├── 启动演示.cmd             # Windows 鼠标双击启动演示模式（端口 8787）
├── 启动真实模式.cmd         # Windows 鼠标双击启动真实模式（端口 8788）
├── start-demo.sh           # Linux / macOS 演示模式启动脚本
├── start-live.sh           # Linux / macOS 真实模式启动脚本
├── launcher.mjs            # 固定的 Node.js 启动器入口（纯标准库，免 npm 依赖）
├── 快速入门手册.md          # Markdown 格式快速入门手册（即本文件）
├── 开始使用.html            # 离线直接双击查看的可视化交互式图文操作指南
├── THIRD_PARTY_NOTICES.md  # 第三方开源协议与许可证说明
│
├── app/                    # 编译后的自包含完整可运行应用（免安装直接运行源）
│   ├── web/                # 预编译纯静态网页文件（Vue 3 + SVG + 科技蓝样式）
│   ├── server/             # Node.js 后端服务脚本
│   ├── engine/             # 预编译核心纯函数引擎
│   └── vendor/             # 内置运行依赖（包含离线 WebSocket 库与 Vue 运行时）
│
├── source/                 # 完整开发源码库（供技术人员二次开发）
│   └── liteCodeTool/       # 源码根目录（包含 package.json, src/, server/, tests/ 等）
│
├── 界面预览/               # 最新版本的真实界面截图
│   ├── 01-属性检查器置顶卡头.png
│   ├── 02-副标题就地双击编辑.png
│   ├── 03-显示态悬停不误触.png
│   └── 04-主动点击展开换船面板.png
│
└── bin/                    # [可选] 便携版可在此放置 node.exe 成为免装环境全离线包
```

---

## 五、常见问题与故障排查

1. **双击启动黑框闪退或提示找不到 Node.js？**
   - 本包运行需要电脑安装基础的 Node.js（推荐 20 或 22+ 版本）。请访问 [nodejs.org](https://nodejs.org) 下载安装；
   - 若您获得的是全内置免安装版（含 `bin/node.exe`），可直接运行无需单独安装 Node.js。
2. **启动后浏览器没有自动打开？**
   - 部分安全软件可能拦截脚本唤起浏览器，请在命令行中查看打印的地址（例如 `http://127.0.0.1:8787/#viewer`），复制到 Chrome、Edge 等现代浏览器中打开即可。
3. **提示“端口已被占用”？**
   - 默认端口 8787（演示）或 8788（真实模式）可能被其他程序使用；
   - 可以在终端中通过环境变量指定新端口启动，例如：`PORT=9090 node launcher.mjs`。
4. **制作好的大屏保存在哪里？**
   - 运行时用户修改的大屏配置保存在与程序同级的独立外部目录 `liteCodeTool_datas/` 中，实现了数据与程序本体彻底解耦，升级或替换本程序时只需保留该数据目录即可。
