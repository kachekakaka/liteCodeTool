# LiteCodeTool 快速入门

## 启动

内网开发请使用完整 Windows x64 离线开发包，运行“打开开发环境.cmd”，在 VS Code 按 F5 选择“前后端联调（演示）”。完整包已带项目依赖，不需要联网安装；详细准备步骤见 [快速开发手册](./快速开发手册.md)。

已有开发环境时运行 `npm run dev:demo`，打开 `http://127.0.0.1:5173`。发布前执行 `npm run build`，再执行 `npm run start:demo`，打开 `http://127.0.0.1:8787/viewer`。地址会自动转换为具体大屏地址。

双平台免安装运行包已内置 Node.js 22，解压和启动方式见 [运行包契约](docs/prototypes/运行包契约.md#包内结构)，无需另装 Node。真实模式不注入演示数据，未收到数据的字段显示 `--`。真实接入凭据只在服务端或外部数据目录配置，不能放进前端。

## 编排和展示大屏

打开根地址或导航中的“大屏管理”，可搜索、新建、重命名、复制、删除大屏，并分别进入编辑或预览。重命名不改变访问地址；复制使用已保存内容，独立保存实例配置，继续共享模板库。可以删除最后一份大屏，空列表仍可新建，重启不会复活已删除的大屏。

从左侧点击或拖入组件模板，用卡片左上角手柄移动，用右下角手柄调整大小。点击内部控件，在右侧修改字段、格式或图表配置；卡头显示、主标题和副标题始终在属性面板顶部。

标题和静态文本可双击就地编辑，回车或失焦提交到当前草稿，Esc 取消；仍需点击大屏“保存”才能持久化。清空副标题会将其隐藏。组件实例的私有修改不影响源模板。

点击“显示态”锁定排版。鼠标悬停卡片浮现切换监控对象按钮，点击后选择目标及来源；大屏编辑态右侧属性面板使用同一份指派。对象切换立即影响当前显示，保存后才成为默认配置。目标列表按业务目标合并，不把同一批号的各个来源重复列出。

来源选“跟随曲线配置”时，各曲线沿用模板设置；“自动选择”固定使用当前目标的一条来源记录，界面显示实际来源；选择具体来源后，缺少该来源会留白并提示，不会偷偷换到另一来源。目标与来源可以一起撤销、恢复最近保存或保存后重开。

“导出”包含大屏与引用模板，“导入”创建独立副本。数据状态页可查看数据模式、实体底账和连接状态；演示模式支持暂停推流与模拟发现新船。

## 模板、地址与未保存修改

### 集成到其他系统

在管理列表点击“地址与嵌入”，复制独立展示地址、嵌入地址或 iframe 代码。每份大屏的嵌入地址为 `/screens/:screenId/embed`，允许任意站点承载。请从集成电脑能够访问的局域网地址进入管理页再复制链接，避免把本机回环地址交给其他电脑；iframe 父容器须有明确高度。HTTPS 父页面应使用浏览器可加载的 HTTPS 大屏入口。

嵌入页保留目标、来源切换和实时数据；选择仅当前页面生效，刷新恢复已保存默认值，多个 iframe 互不影响。嵌入页不提供保存、文字编辑或返回编辑器入口，`Ctrl+S` 不保存，`Esc` 关闭浮层。修改默认配置请回到管理端编辑并保存。可重复的集成验收见 [iframe 专项用例](tests/iframe-embedding.md)。

### 编辑与草稿

进入组件工坊后，新建模板、添加原子控件、声明对象槽位、配置数据字段，再保存入库。模板不固化具体船舶身份；修改已有模板会影响引用它的大屏，需要独立修改时使用“另存为新模板”。

需要高度曲线时，在工坊选中曲线，将数据类型切换为“飞行目标”，Y 轴即可选择“高度 / km”和“速度 / km/h”，分别配置左、右轴。船舶模式保留航速 / 节。若大屏实例所属模板没有飞行目标槽位，先在工坊建立并保存该槽位。工坊的“预览目标”与来源只用于试看，不触发未保存提示，也不写入模板。

浏览器地址可直接定位大屏和模板。旧 `#viewer` 等地址仍可转换；无效地址明确提示错误，不自动换成其他大屏。

在大屏、工坊和数据页之间切换会保留当前大屏与模板各自的草稿；打开另一资源或重新新建时会提示是否放弃修改。刷新或关闭浏览器会提示未保存，但不会自动恢复草稿。将地址复制到新窗口，只能看到已保存的内容。

快捷键：`Ctrl+S` 保存当前编辑对象，`Ctrl+Z` 撤销大屏修改，`Ctrl+Shift+Z` 重做，`Esc` 关闭交互或从显示态返回编辑态。

## 数据与故障排查

源码、临时产物与用户数据分开存放。不要把真实配置、凭据、业务数据复制进源码或离线交付包；演示模式使用单独数据子目录。

更新代码后需重新启动服务，旧数据目录会自动补齐缺少的飞行目标模式；仅修改内置默认值无法更新已存在的旧模式文件。遇到单位或身份冲突时服务会停止升级并报告原因，不要通过删除用户数据目录解决。升级备份和接入单位设置见[快速开发手册](快速开发手册.md#飞行目标接入与旧目录升级)。

启动失败时查看终端中的端口、Node 和数据目录错误。页面能打开但显示数据过期时，应检查推流来源与最后更新时间；“连接正常”不代表业务字段已更新。深层地址刷新失败时，确认运行的是本项目服务端或配置了前端页面回退的服务器。
