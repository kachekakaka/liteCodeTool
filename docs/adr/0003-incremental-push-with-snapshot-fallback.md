# 初始底账拉取与增量推流结合的数据完整性架构

纯依赖 WebSocket 推流会导致前端在大屏刚加载、或未收到该字段推送时处于盲等和数据空白状态；前端绝不能凭空捏造数据。我们决定采用“HTTP 查初始底账 + WebSocket 听增量改写 + 空值占位符”的标准数据流：当大屏加载或组件绑定某艘船时，前端首先通过 HTTP 接口拉取后端数据库当前记录的该船全量最新状态（初始底账）填入实体池；后续 WebSocket 仅推送变化的字段覆盖对应属性；若后端数据库本身就未曾记录过该字段，则前端严格呈现“--”空值占位状态，杜绝任何数据臆测。
