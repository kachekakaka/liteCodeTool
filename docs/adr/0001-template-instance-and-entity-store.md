# 组件模板与实例解耦及全局实体数据池架构

低代码大屏需要支持在设计态复用通用组件，并在运行态动态指派和切换监控实体（如特定 AIS 船舶），同时应对 WebSocket 高频数据推送。我们决定将组件严格划分为“组件模板（通用契约）”与“组件实例（画布私有副本）”，并在前端采用“全局实体数据池（Entity Store）”进行响应式数据订阅分发。这样既保证了大屏运行态修改或保存具体对象绑定时绝不污染原始组件库，又避免了高频推流时递归遍历组件树的性能瓶颈。
