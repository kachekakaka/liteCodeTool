# 基于文件系统的纯 JSON 本地持久化选型

为了确保低代码平台的极致轻量化与零环境安装依赖，我们决定不采用重量级数据库（如 MySQL、PostgreSQL）或需要原生编译的嵌入式数据库（如 SQLite），而是直接采用纯 JSON 文件系统持久化。所有大屏配置、组件模板定义及模拟实体底账数据均以标准格式的 JSON 文件保存在仓库同级外部数据目录（`../liteCodeTool_datas/`）中，读写直观透明，彻底实现代码与持久化数据的物理隔离。
