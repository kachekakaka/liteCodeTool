# 项目协作与 Agent 行为约束

## 🇨🇳 语言与沟通规范
- **日常交流、代码注释和提交说明**：严格使用简体中文。
- **与用户沟通**：使用通俗易懂的日常语言。提供技术实现选项时，先用日常语言说明每个选项的实际效果、优缺点和适用场景，再补充必要的专业术语，并明确给出推荐及理由；严禁仅罗列技术名词让用户做选择题。

## 🚫 授权与文件红线
- **授权改动原则**：只有用户明确要求“修改”“修复”“实施”或“写代码”时才修改仓库文件；未获授权时只进行只读分析与方案规划。
- **本文件保护**：未经用户明确要求不得修改本文件；本文件只保留跨任务、长期稳定的协作红线与权威指针，不得写入或复制具体实现参数、临时命令或阶段性事实。
- **原型/契约真源**：若后续引入交互界面，交互原型唯一真源为 `docs/prototypes/prototype.html`，新页面在此文件内原地追加导航 Tab 演进，严禁新建散落的 HTML 副本；若是 CLI 或脚本工具，则以原型/契约文档为真源。历史状态交给版本控制，不做手工归档复制。
- **轻量实用，杜绝过度治理**：坚决不引入成套重型测试体系（UI 测试、E2E、复杂 CI 门禁等）；保持极简实用主义，仅允许对底层关键核心算法、解析转换或纯函数逻辑编写轻量、短小、针对性强的单测。
- **临时与过程资产隔离红线**：所有临时日志、过程草稿（如任务跟踪 `.scratch/`、调试日志 `*.log`、缓存、测试临时输出等）真源一律存放至仓库同级外部目录 `../liteCodeTool_tmp/`（即 `[仓库名]_tmp`），严禁在代码仓库根目录下遗留临时文件，确保代码仓库始终纯净规范。
- **持久化用户数据隔离红线**：运行时用户配置文件、外部持久化数据一律指定或存放至仓库同级外部目录 `../liteCodeTool_datas/`（即 `[仓库名]_datas`），实现数据与应用代码彻底解耦。
- **成果报告双写镜像与物理落盘铁律**：为兼顾 IDE 界面渲染与项目受控归档双重需求，实施成果报告（如 `walkthrough.md`、验收记录等）严格执行**双写镜像规范**。系统内部生成 Artifact 后，**必须在同一时刻通过命令将其物理同步镜像复制至项目指定的同级外部临时目录 `../liteCodeTool_tmp/walkthrough.md`**；只有外部临时目录物理存在最新成果报告时，实施才算合规闭环；严禁仅留存于内部缓存，确保用户能在项目同级直观查阅所有过程产出。

## 📋 方案生命周期规范
- **前置征询（先问再做）**：涉及复杂重构、底层协议改造或多模块联动任务时，先用日常语言说明改动范围与影响，并主动询问用户：“是否需要先输出一份实施方案？”；只有用户明确同意或要求时才编写方案；若用户指示“直接做/直接写代码”，则跳过方案直接进入实施。
- **真源落盘（严禁写临时目录）**：方案是项目的正式受控资产，严禁写入 AI 内部缓存或外部临时目录。经用户确认输出的方案统一存放在 `docs/方案/`（命名规范：`YYYYMMDD-任务名称.md`），明确技术路径、改动文件与验收标准。
- **完成即归档**：方案实施并验证通过后，必须闭环归档：将该方案文件移动至 `archive/docs/方案/`，并在文件头部注明完成日期与验证结论，确保活动方案目录清爽、历史轨迹可追溯。

## 🔧 Git 行为规范（前期未初始化 Git 时规则预备，后续一旦启用全量强制执行）
- **绝不自动提交**：不自动执行 Git commit、push 或创建 PR；只有用户主动要求时才做。
- **提交信息格式**：后续执行 Git commit 时，提交说明严格遵循简体中文格式：
  - `feat: 新增功能`
  - `fix: 修复问题`
  - `chore: 杂项`
  - `docs: 文档`
- **Windows PowerShell 提交防乱码铁律**：PowerShell 下含中文的提交说明先写入临时 UTF-8 无 BOM 文件，再使用 `git commit -F <文件路径>` 执行提交，切勿直接在命令行中使用 `-m "中文"` 传入或通过管道传入，彻底杜绝乱码。提交完成后清理该临时文件。
- **提交内容安全**：严禁提交任何密钥、Token、本地临时日志、用户本地数据（`../liteCodeTool_datas/`）或外部临时文件（`../liteCodeTool_tmp/`）。
- **进程控制**：同一时间只运行一个 Git 相关 shell 任务，不得并行反复扫描，只结束已确认属于本会话的遗留进程。

## 📖 事实与真源边界
- 当前文档遵守“一项事实一个真源”；文档导航与生命周期按 [`README.md`](README.md#文档导航) 执行，其他文档只链接、不复制。
- 开发和代码修改须遵循 [`快速开发手册.md`](快速开发手册.md) 中的架构分工、格式化、注释、调试与按改动影响范围验证的规则；重大构建取舍查阅 [`docs/adr/`](docs/adr/)，不在本文件重复维护技术细节。
- 业务名词与概念统一查阅 [`CONTEXT.md`](CONTEXT.md)，严禁自造别名或在代码中混淆术语；ADR 只记录长期不可逆的重大决定（统一归档在 `docs/adr/`）。
