# liteCodeTool：v0.2.0 合并方案审核意见

审核日期：2026-09-06  
审核分支：main  
锁定提交：8a957d914924da6df988646d42a81dc0ad56316a  
提交对应文件树：cc1b7d904c6d2e2238132120231217c5015ecd05  
性质：只读方案审核与交付源码对照，不是新实现的运行验收；没有修改或推送仓库。

## 结论

方向认可，建议修订 V1.0 后实施，不建议按现有文本直接认定“差距全部补齐”。应保留平铺合库、沿用现有软件、全局时序序列、可配置表格陈旧提示、全局抽屉与局部快捷切换、双轨构建等方向。实施前需要补足界面配置、数据时效语义、构建产物启动验收；同时明确合库清单、F2处理和验收/归档边界。

## 审核依据与验证范围

1. 本次实际通过 GitHub 读取 main 与指定提交，确认 main 指向上述完整 SHA。该提交是方案、差距清单、ADR 和术语变更；远端尚未包含本次计划合入的应用源码。
2. 阅读 `docs/方案/20260906-v0.2.0源码合并与差距补齐实施方案.md`（V1.0）全文、ADR 0007、ADR 0008、AGENTS.md 的协作约定以及本次纳入的差距对照表。
3. 直接打开既有 `LiteCodeTool_v0.2.0_可运行软件包_含源码.zip`，核对 types、core、Inspector、LineChart、构建脚本、服务入口和启动器等源码。
4. 实际计算该 ZIP 的 SHA-256 为 `07e69773adaa9c35383625611e9070a55fad73d21abb0276138cbb0965d782f4`，与仓库差距清单末尾记录一致。
5. 针对新方案的 max(timestamps) 行级公式做了一个确定性反例，并用原包 EntityStore/resolveValue 对照。没有编造新的实现测试通过数量；没有重跑整套软件验收。

## 一、实施前应补齐的三个关键问题

### R1 / P1：全局曲线缺少明确的属性配置改动

**方案位置**：实现决策 2；用户故事 2。

**已写内容**：扩展 Series、LineChart.vue 读取 `_global` 历史、core.ts 放宽合法全局数值字段的校验。

**缺口**：没有明确把 Inspector.vue 的曲线配置列入 F1 改动。原包 Inspector.vue 第 37–40 行的 seriesAdd 依赖模板槽位；没有槽位就提示“请先声明对象槽位”。第 69 行的曲线表单只提供槽位和字段选择，numericFields 也依赖 slotId。因此只按计划列出的底层改动，用户仍无法通过正常界面制作一个无槽位的全局曲线模板。

**建议补写**：
- 每条曲线提供“对象槽位 / 全局数据”来源选择；全局模式只列出 isEntity=false 的数据模式及数值字段。
- 全局序列不要求模板声明对象槽位；新增、切换来源、删除、标签和颜色都可以在检查器完成。
- 切换来源时清除另一来源的无效标识；target 缺省按 slot 兼容旧配置；未知 target、不匹配的 schemaType、缺失槽位和非数值字段应明确拒绝。
- 不仅模板能保存，大屏的实例私有覆盖、导入导出、重启回读也应接受同一契约。
- 保留现有同图单位不兼容提示、空历史留白、null/断流不连线和快照不补历史的行为，不必另加双坐标轴或历史数据库。

**最小验收**：新建零槽位模板，在界面添加 port_stats.total_vessels；保存、放到画布、接收增量、观察曲线；刷新后配置仍在但不伪造跨会话历史；修改一个实例不改变源模板；旧 slotId 曲线保持可用。

### R2 / P1：max(timestamps) 只能判断整条记录停更，不能表示每列都新鲜

**方案位置**：实现决策 3；ADR 0008；差距 F3。

**问题边界**：按行最新时间判定“整条记录是否停更”本身是合理定义，并非该公式一概错误。但它不能完成差距表所述的字段级质量对齐，也不能证明旧航速/旧状态仍是当前信息。

**本次实测反例**：阈值 120 秒；航速和状态最后更新在 300 秒前，经度在 1 秒前更新。方案行级公式得到 stale=false；原包数值卡针对航速的 resolveValue 得到 stale=true。同一艘船会出现“卡片航速已过期、表格航速/在航仍没有过期提示”的不一致。

**建议补写**：
- 保留记录级最近更新作为整行停更提示，同时对需要时效判断的动态展示字段使用对应的源时间戳；静态身份信息不要无差别参与最小时间判旧。
- 行级和字段级质量分开，不把“某列最近更新”解释成“全部列已刷新”；也不要改写后端的业务状态为“离线”。
- 若本轮只做整行停更，明确把“字段级陈旧提示”保留为未完成项，不能标记 F3 的质量对齐全部完成。
- 缺失字段保持 `--`；缺少时间戳显示未知，不默认为实时；关闭提示只代表不监测，不能显示“实时正常”。
- 陈旧计算必须随时钟更新，不可只在收到新推流时计算，否则完全停流时不会自然进入过期状态。
- 核心判定提取到 core.ts 的公开纯函数，Vue 只负责调用与呈现，使测试和实际表格使用同一实现。

### R3 / P1：双轨构建必须各自有明确的产物和启动验收

**方案位置**：实现决策 5；ADR 0007；用户故事 6、7。

**原包对照**：
- tools/build.mjs 输出 `../liteCodeTool_tmp/release/app/`，包含 web、server、engine 等离线运行文件。
- vite.config.ts 输出前端到 `../liteCodeTool_tmp/dist/`。
- package.json 的 demo/start/preview 指向离线 release/app/server/index.mjs。
- server/index.mjs 默认从 root/web 读取前端，也支持 CLIENT_DIR。

**风险**：只看到 build:vite 成功，然后运行 npm run demo，会实际启动另一条轨道的离线产物。这样不能证明 Vite 产物可用；陈旧的旧产物还可能掩盖新构建的错误。此处并不是说 Vite 产物无法启动，而是方案尚未明确它的启动路径。

**建议补写**：
- 写清离线构建和标准构建各自的前端目录、后端入口、CLIENT_DIR、数据目录与启动命令；支持源码服务或预编译服务均可，但必须指定。
- 在干净环境分别构建并启动两套产物，检查首页、静态资源、HTTP、WebSocket、保存、重启回读；禁止一轨失败后静默改用另一轨。
- 标准路径正常安装并提交锁文件，执行完整 vue-tsc；固定离线依赖与标准依赖的版本关系，保留许可文件。不同版本时如实列明，不能默认“两轨完全相同”。
- “2 秒完成”仅能作为指定设备的实测参考，不能成为跨机器承诺；同理单测 <500ms 不替代行为验收。
- Windows 浏览器直接访问 HTTP/WS 的原生验收必须单独记录，不以临时核验桥代替。

## 二、合库与验收边界还需补写

### R4 / P2：平铺合库需要覆盖规则、发布映射与路径校正

**认可**：一个仓库、一套应用源码，不再引入第二套工程。

**需要具体化**：
- 以当前 `8a957d9` 为合库目标基线，而不是以交付包的 `8dd3a9f` 状态覆盖 main。实施前再检查 main 是否已有新提交。
- source/liteCodeTool 包含 README.md 和与远端同名的实施方案文件。明确这些冲突采用逐节合并而非无条件覆盖；保留既有 AGENTS、CONTEXT、ADR、新合并方案和唯一 HTML 原型。
- 列出源码导入白名单、文档人工合并清单与不得提交项。保留 THIRD_PARTY_NOTICES.md 和 vendor 的许可；不得提交用户数据、凭据、临时日志与核验桥。
- 方案的 `src/engine/runtime.ts` 与包内实际的 `src/runtime.ts` 不一致，应修正路径，或明确另有迁移任务，不能无说明地多建一份状态机。
- 原包两个 Windows `.cmd` 在发行包顶层，不在 source/liteCodeTool 内；tools/launcher.mjs 说明是“复制到发行包根目录使用”，实际寻找 app/server/index.mjs。平铺源码并不会自动生成发行布局。
- 明确从受控源码生成发行目录、复制启动脚本/launcher、构建 app、制作 ZIP 的步骤；交付包不能依赖开发机另一个旧目录中的产物。
- 建议源码原样合入、功能补齐、构建发布三个可审查阶段；工作分支正常合并，不强制覆盖主分支。

### R5 / P2：F2 的处置必须写清；F4 可保留为明示替代

新方案针对 F1、F3 给出了实施决策，对 F4 明确选择“全局抽屉 + 局部快捷切换”。这个交互选择可以保留，无需为形式一致重复建设第二个微型搜索框；方案所称“Q5裁决”的原始记录本次未提供，可补来源引用，但不能据此断言不存在该确认。

F2 则仍未被完整处置。排除复杂 SQL/AND/OR，不等于处置了“表格显式槽位集合”和“曲线按单个条件动态纳入实体”。它们不是复杂查询语言，但原始需求使用“或”，也不能无依据地要求这两种组合本轮都必须实现。

建议新增范围对照：

| 缺口 | 本轮处置 | 剩余边界 |
| --- | --- | --- |
| F1 | 补齐全局时序曲线，包括界面配置与持久化 | 不自动扩为跨会话历史数据库 |
| F2 | 明确实施或显式延期；若保留现状，则保留表格条件集合、曲线固定槽位集合 | 未提供的另一组合仍记在清单，不能因排除复杂 SQL 而消失 |
| F3 | 明确行级与字段级时效范围及可关闭规则 | 只完成行级时，不宣称逐字段质量统一 |
| F4 | 采用全局抽屉检索、局部快捷切换 | 明示接受交互差异，不重复计作缺少换船功能 |

### R6 / P2：配置契约、测试层次和归档条件需要一致

**配置命名**：ADR 0008 和 CONTEXT 使用 staleSeconds，方案新增 tableStaleSeconds/tableStaleEnabled，原包已有 staleSeconds。可以选择表格专属字段，但必须统一文档、类型、表单、校验与读写；定义旧配置的继承/迁移，不让同一阈值出现两个事实来源。

**建议规则（审核建议，不是原稿已有内容）**：旧表格缺省字段按明确默认值处理；关闭开关优先于秒数；0 表示不监测；负数、非有限值和错误类型拒绝；单值卡片保留原语义，不能顺手改变既有行为。若保留“<=0关闭”，也应说明为什么接受负值，并统一校验，不允许一端关闭、另一端报错。

**测试**：保留已有纯函数测试，追加旧 slot 配置、全局正常/非法字段、无槽位全局模板、实例覆盖、部分字段停更、完全停更、0/关闭/缺省、缺时间戳、保存回读。以行为覆盖为准，不仅是“22+3通过”。新增逻辑不能全写在 Vue 内，再用与界面无关的另一个函数通过单测。

**测试约定**：AGENTS.md 不引入成套 E2E。新方案的 HTTP/WS Seam B 应明确为必要的轻量运行核验、记录和临时脚本放在外部临时目录，不误写成新增长期端到端测试体系。需要修改长期约束时应另有授权，不能靠方案措辞隐式覆盖 AGENTS。

**分级完成**：分别列明“源码合入”“F1/F3约定范围补齐”“双轨构建通过”“Windows原生启动通过”“真实JSON源联调”“目标规模长时运行”“实际大屏设备”。真实AIS/NMEA解码可在范围外，但结构化源联调、长时性能和设备验收仍应标注实际状态；本轮未承担的转为明确遗留任务。

**归档**：本次窄范围合并任务通过可归档本方案；原始整体实施方案只有对应条目也验证完成，才能标记整体完成。不能用本次纯函数或编译结果宣布现场验收通过。

## 三、其他文档整理建议

- 把 file:///d:/Projects/... 链接改成仓库相对链接，避免其他人的电脑无法导航。
- CONTEXT 保存长期术语，参数名称与默认值放到契约真源，避免多处重复不同参数。
- 新增全局来源和表格陈旧设置后，同步唯一 docs/prototypes/prototype.html 的相关交互区域或明确其契约地位，避免原型与实现相互矛盾。
- 本地软件仍需要 Node.js，不应把“一键启动”写成“免运行环境”；不新增免 Node 安装器作为本次隐含需求。
- 方案提到局域网部署，不意味着当前 localhost 程序已完成多人访问和安全验收。保留默认本机监听；局域网场景另行明确绑定地址、允许主机和访问边界，不应直接转为公网服务。

## 四、给实施者的最小收尾顺序

1. 修订上述 R1–R6，并明确 F2、F3 的验收范围。
2. 基于当前 main 在工作分支合入核对过指纹的源包，保留现有文档与许可；先复现离线构建和原有核心测试，区分基线缺陷与新增改动。
3. 补全局曲线的类型、公共解析、检查器、渲染、校验与保存；补表格时效的统一语义、属性与展示。
4. 分别完成离线和标准构建启动；打出新发行包，从最终解压目录做 Windows 原生启动和保存回读核验。
5. 回读远端提交并记录交付 SHA；对现场数据、性能和实际设备分别标注通过或未验收，再按相应范围归档。

本报告只是审核意见，不自动替换受控实施方案。需要实施时，应将接受的修订正式写入仓库中的方案与相关 ADR。
