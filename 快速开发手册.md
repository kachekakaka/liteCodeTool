# LiteCodeTool 快速开发手册

## 打开项目与调试

项目采用 Vue 3、Vue Router、TypeScript、Vite 和 Node。开发和发布使用同一条 Vite 构建链；第三方版本以 `package-lock.json` 为准。统一构建与完整离线开发包的取舍见 [ADR 0012](docs/adr/0012-vite-build-and-offline-development-kit.md)。

外网开发机安装 Node（至少 22.16）后运行 `npm ci`，在 VS Code 打开仓库根目录，安装推荐的 Vue 官方扩展和 Prettier 扩展。内网开发机使用准备好的完整离线包，无需运行 `npm install`。

Node 程序不存入 Git 代码仓库。离线包根目录的 `toolchain/node/node.exe` 是配套运行环境，npm 位于其 `node_modules/npm/` 中；源码目录 `liteCodeTool/` 与 `toolchain/` 并列。通过“打开开发环境.cmd”启动时会优先使用包内 Node，不需要在内网额外下载安装。

按 F5 选择“前后端联调（演示）”：后端和 Vite 就绪后启动 Edge 调试。前端默认 `http://127.0.0.1:5173`，演示后端 8787；真实联调使用后端 8788。只调前端时先启动开发服务，再选择对应浏览器配置；只调后端时选择独立后端配置。

断点示例：`src/stores/screens.ts` 的 `saveScreen`、组件中的点击处理函数、`server/index.mjs` 的 `/api/bootstrap` 分支。Vue/TypeScript 断点应落到源码；灰色断点先检查文件路径、开发服务和浏览器调试会话是否对应。结束联调会停止本次启动的服务及浏览器，不结束其他进程。

真实接入凭据通过服务端 `INGEST_TOKEN` 环境变量设置，不能写进 `.vscode`。使用真实模式时不会自动生成演示实体；运行包的启动器可在数据目录准备 `connection.json`。开发与发布数据默认放仓库同级 `liteCodeTool_datas`，演示数据另分 `demo` 子目录。

终端使用 PATH 中的 PowerShell 7（`pwsh`）。调试时可设置 `LITECODE_DATA_DIR` 指定外部数据目录；命令行开发服务使用 `DATA_DIR`。`PORT` 和 `VITE_PORT` 可分别调整后端和前端端口；前端调试配置 URL 须同步修改。开发与发布默认监听所有 IPv4 网卡，后端不再限制 Host/Origin，前端 API 和 WebSocket 仍使用页面自身地址。真实数据接入的 `INGEST_TOKEN` 要求保持不变。

## 常用命令

| 命令 | 用途 |
| --- | --- |
| `npm run dev:demo` | 演示开发服务和热更新 |
| `npm run dev` / `npm run dev:live` | 真实模式开发服务 |
| `npm run typecheck` | Vue 与 TypeScript 类型检查 |
| `npm run format` / `npm run format:check` | 格式化 / 只检查格式 |
| `npm test` | 核心算法与路由纯函数单测 |
| `npm run build` | 类型检查、Vite 构建和发布装配 |
| `npm run start` / `npm run start:demo` | 运行上次构建的发布产物 |
| `npm run package:offline` | 使用已准备的工具链资源装配离线开发包 |
| `npm run prepare:runtime` | 联网准备双平台 Node 22 运行资源 |
| `npm run package:release` | 离线构建并装配 Windows x64、Linux ARM64 运行包 |

`build:vite`、`start:vite`、`start:vite:demo` 是兼容别名，不再产生另一套构建结果。Vite 产物为 `../liteCodeTool_tmp/dist`，装配好的运行代码为 `../liteCodeTool_tmp/release/app`；运行包启动器在 `release/launcher.mjs`。

## 代码放在哪里

| 目录 / 文件 | 职责 |
| --- | --- |
| `src/App.vue` | 路由容器、全局错误、通知、弹层与离开保护 |
| `src/router/` | 资源地址、旧地址转换、导航保护、资源装载 |
| `src/views/` | 大屏编辑、显示、组件工坊、数据状态及错误页面 |
| `src/layouts/` | 公共页面布局 |
| `src/components/` | 导航、工具栏、资产库、画布、卡片及抽屉 |
| `src/components/inspector/` | 按卡头、字段绑定、曲线、表格等拆分的属性区域 |
| `src/components/controls/` | 表格和图片等独立原子控件渲染 |
| `src/composables/` | 画布缩放拖拽、快捷键、路由导航和当前编辑对象操作 |
| `src/stores/` | 大屏草稿、模板草稿、实体数据、应用界面状态 |
| `src/services/` | HTTP、WebSocket、初始化和演示动作 |
| `src/engine/core.ts` | 实体池、字段求值、配置校验等纯逻辑 |
| `src/styles/` | 主题、基础、布局、控件、画布、检查器、弹层与响应式样式 |
| `server/` | REST API、持久化、静态页面托管和开发协调 |
| `tools/` | 标准构建、运行和离线包准备 |

页面、公共布局、业务容器和展示组件按职责组织。`App.vue` 承担路由容器和全局交互，不重新承载整页业务分支、拖拽或模板编辑。复杂属性区域和控件分别组织，不能把巨型页面或检查器整体搬进另一个文件来代替拆分；简单展示可以适度合并，不按固定行数或组件数量设门槛。

路由决定页面模式和资源身份，页面及导航协调层调用 store，store 不导入 router；底层 HTTP、实时传输和核心算法不依赖页面。沿用 Vue 响应式能力组织现有轻量 store，不要重新创建一个集中承载所有状态和页面动作的 runtime 文件。

展示组件优先通过明确的 props 和事件协作，少量业务容器可以使用对应 store；避免全量展开全局状态或大量透传整个业务对象。搜索框、展开项、就地输入等局部交互状态归所属组件，需要跨页面保留的业务草稿归对应 store。

新建或实质改写的组件采用标准 `<script setup lang="ts">`；现有组件随实际职责调整迁移，不为统一语法额外扩大改动。组件专属样式随组件维护，跨组件样式明确作用范围；调整 `scoped` 或加载顺序时核对既有样式，避免选择器失效。

新增页面：在 `views` 创建页面，在 `router/index.ts` 注册命名路由，导航使用 RouterLink；如果引入资源草稿，补上替换保护及错误处理。新增控件：扩展 `types.ts` 与核心校验，在 `ControlRenderer.vue` 注册渲染，在 `components/inspector` 增加对应设置，在 `server/defaults.mjs` 提供原子模板。不要把新功能继续堆进根页面。

修改交互契约时，只在 `docs/prototypes/prototype.html` 原地演进；业务术语查阅 `CONTEXT.md`。只对核心纯逻辑补充短小单测，不引入 UI/E2E 测试框架。

## 格式化与注释

自有源码遵循仓库现有 Prettier 配置，VS Code 保存时格式化与命令行格式检查使用项目本地同一套工具。具体格式参数以 [`.prettierrc.json`](.prettierrc.json) 为准，编辑器接入见 [`.vscode/settings.json`](.vscode/settings.json)；修改时保持格式一致，不另设一套手工规则。

自有源码的具名函数和方法使用 `/** ... */` 多行文档注释，说明用途，用 `@param` 解释每个入参的含义、单位和默认行为，用 `@returns` 说明实际返回值。异步方法需区分“流程已完成”和“仅启动回调”，说明取消、失败时的结果；会抛出关键异常的方法补充 `@throws`。TypeScript 的 `interface`、`type` 及字段说明数据含义、可选值和单位，泛型使用 `@typeParam`。修改方法签名或行为时同时维护注释，VS Code 悬停可直接查看这些说明。

## 路由与草稿

大屏管理地址为 `/screens`，根地址 `/` 进入管理页；编辑地址为 `/screens/:screenId/edit`，显示地址为 `/screens/:screenId/view`，嵌入地址为 `/screens/:screenId/embed`。模板为 `/workshop/:templateId`，新建模板为 `/workshop/new`，数据页为 `/data`。`/viewer` 解析有效大屏，无大屏时进入管理页；`/workshop` 返回当前模板并转换为标准地址。旧 `#viewer` 等入口及根入口 `?screen=...` 仍可转换。

当前大屏和模板各保留一份内存草稿。同一资源切换页面不丢草稿；替换资源时提示，取消后保持原地址和内容。刷新、关闭时使用浏览器原生提示，不自动保存。复制地址到新窗口只加载已保存内容。无效资源明确报错，不用默认资源顶替。

应用初始化负责元数据、资产和实体底账，路由负责显式资源装载。初始化由入口和路由守卫共享的幂等服务负责，不等待 App 挂载。资源请求采用序号和取消机制，旧响应不能覆盖新目标；等待切换时暂停当前页面交互，防止确认后又编辑旧草稿。

大屏允许空集合，`/api/bootstrap` 的 `screen` 此时为 null；初始大屏只在一次性初始化中建立，不能在每次启动时补建。删除使用 `DELETE /api/screens/:screenId` 和读取时的 `If-Match`，与保存共用资源文件队列；204 无响应体。管理重命名成功后同步当前草稿、保存基线和 ETag，避免下一次保存冲突。

嵌入路由复用展示视图，以 `embedded` 上下文限制保存和文字编辑。临时指派只在页面内存生效，不写本地存储和服务端；进入/退出嵌入上下文会重置相应状态。发布服务对展示和嵌入地址允许任意父站加载。轻量跨来源宿主与实际浏览器验收步骤见 [iframe 专项用例](tests/iframe-embedding.md)。

全局 WebSocket 及重连、时钟等配套任务由应用生命周期统一管理，切换页面不反复建立连接或清空历史。表格自动翻页、长按、拖拽及快捷键等局部交互的计时器和监听归所属组件或 composable，随对应生命周期创建和清理。全局与局部资源各自处理销毁及热更新清理；初始化失败后重试时防止重复连接、监听或计时器。

## 飞行目标接入与旧目录升级

飞行目标的模式使用 `targetIdField: "batch_no"`、`sourceField: "source"`，不再把业务批号作为 `idField`。`Envelope.type + id` 唯一标识一条来源记录；同批号不同来源必须使用不同的记录 ID，同一批号与来源只能对应一条记录。记录建立后，不允许用增量更换批号或来源；后续只上报变化字段即可。船舶仍保留原有 MMSI 身份约束。

接入示例（timestamp 替换为实际毫秒时间戳）：

```json
{"type":"projectile","id":"P-101-radar2","timestamp":1788940000000,"data":{"batch_no":"P-101","source":"雷达2","altitude":1,"speed":36}}
```

平台快照、历史、阈值及展示统一为高度 km、速度 km/h。新目录默认按平台单位接入；旧单源首次接入未给批号时采用记录 ID，未给来源时作为无来源单源。多来源首次接入应同时提供批号与来源。真实接入使用 `POST /api/ingest` 及服务端 `INGEST_TOKEN`，演示模式禁用外部接入；演示数据与真实接入共用身份校验。

旧目录升级在获得当前目录锁后、载入模式和快照前运行。只补齐缺少的内置模式、字段和模板，保留用户已有配置；真实目录不补演示实体。已知旧单位 m、m/s 会转换快照及明确关联的数值规则，未知单位、身份冲突或无法解析的表达式会在写入前报错。升级原文备份保存在该数据目录 `.upgrade-v2/journal.json`，完成标记为 `.data-upgrade-v2.json`；中断后下一次启动先恢复原文再重试，完成后再次启动不重复换算。不要手工删除标记或在服务运行时改写备份。

迁移旧单位时同批生成 `ingestion-units.json`，保留旧来源的输入单位。数据源切换为新单位时，可在受控停机期间修改该文件，再重启服务。例如默认继续接收米制，仅雷达2改为平台单位：

```json
{"default":{"altitude":"m","speed":"m/s"},"sources":{"雷达2":{"altitude":"km","speed":"km/h"}}}
```

字段级来源配置覆盖默认值；支持的输入单位仅高度 m/km、速度 m/s/km/h。转换只发生在真实接入入口，读取规范快照及演示推流不再转换。旧单位模式已同时存在自定义输入配置时升级停止，须先核对配置含义。启动时只处理当前选定目录，真实与演示目录分别升级。

大屏 `bindingVersion: 2` 明确来源语义：槽位来源键缺失表示沿用曲线配置，空串表示显式自动，来源名称表示严格指定。无版本旧配置的空串在载入、保存及导入时转换为缺失，保持历史行为。自动选择优先记录 ID 等于业务目标的主记录，否则按记录 ID 排序选择一条；缺失指定来源不回退。按业务目标查询底账可用 `GET /api/entities/projectile/P-101?byTarget=1&source=...`，响应保留实际记录 ID 和字段时间戳。工坊预览独立存于会话状态，旧 `workshopPreviewTarget` 会在配置边界剥离。

## 准备完整离线开发包

准备机与目标机均为 Windows x64。先在准备机安装项目依赖并执行类型检查和构建；准备官方 Node、VS Code、PowerShell 7 的本地程序目录，以及 Vue.volar、esbenp.prettier-vscode 的 VSIX 安装包。准备资源统一放在源码同级外部临时目录，不提交工具链和扩展二进制。

```powershell
node tools/prepare-offline.mjs --vscode-dir '<VS Code程序目录>' --pwsh-dir '<PowerShell 7程序目录>' --vsix-dir '<VSIX目录>'
npm run package:offline
```

可用 `--node-dir` 显式指定 Node 程序目录，否则采用当前 Node 所在目录。准备脚本安装 VSIX 到隔离的扩展目录，生成版本、来源与校验清单；打包脚本不访问网络，缺资源或锁文件变化会明确失败。任何扩展依赖也需要准备齐。

结果位于 `../liteCodeTool_tmp/offline-devkit`，包括源码和完整 `node_modules`、Node/npm、VS Code、PowerShell、扩展与 VSIX。将整个目录压缩带入内网；运行“打开开发环境.cmd”打开独立 VS Code，工具链只注入本次进程，不修改系统 PATH 或已有编辑器配置。浏览器调试需要目标机已安装 Edge 或 Chrome；本包不包含浏览器安装程序。

若将开发包解压在 `liteCodeTool_tmp` 下进行验证，数据目录必须另指定到临时目录之外：

```powershell
.\打开开发环境.cmd --data-dir 'D:\工作数据\liteCodeTool_datas'
```

新增依赖需要回到可联网的同平台准备机，更新锁文件、重新准备工具链清单并重新打包。离线包只能保证已包含的依赖，不会自动从内网或外网补包。真实外部数据源及用户配置的远程图片仍需在目标网络可达。

## 发布与验证

执行 `npm run build` 后，运行产物不需要开发依赖；客户端已预编译，服务端共享核心由标准 TypeScript 编译为 JavaScript。运行 `node ../liteCodeTool_tmp/release/launcher.mjs` 可打开演示显示态，加 `--live` 进入真实模式。要交付内置 Node 的双平台免安装包，按 [运行包契约](docs/prototypes/运行包契约.md) 准备资源和出包；现有开发命令和系统 Node 不受影响。

日常修改按实际影响范围选择验证，不把完整架构改造验收作为所有任务的最低门槛：

| 改动范围 | 对应验证 |
| --- | --- |
| 仅文档 | 内容、文件链接、章节锚点及差异空白检查，无需应用测试或构建 |
| 自有源码 | 检查修改文件格式，涉及 Vue/TypeScript 时做类型检查，并验证受影响行为 |
| 核心算法、解析转换或纯函数 | 运行相关短小单测，必要时补充针对性用例 |
| 构建、依赖或发布装配 | 执行生产构建，核对受影响的产物和启动方式 |
| 路由或草稿行为 | 在开发和生产服务验证受影响的深层地址、刷新、旧地址、无效资源、草稿切换或保存 |
| 调试配置或源码映射 | 实测受影响端的源码断点及调试进程启动、退出 |
| 离线开发工具链或依赖交付 | 重新准备开发包，验证迁移目录及断网后的相关开发能力 |

跨多个范围的改动合并执行相应检查；已有实施方案明确的专项验收仍按其标准完成，不因本表而减免。无需为验证引入 UI/E2E 框架或新的 CI 门禁。

完整断网、前后端源码断点等专项验收用于确认对应能力。离线验证必须实际阻断外网并保持回环连接，不能用单纯设置 npm offline 代替；配置存在、服务能启动或构建通过不能替代这些实测。没有亲自完成的断点或断网检查须在验收记录中列为未验证，不据此宣称完整验收通过。
