# LiteCodeTool · 快速开发手册 (面向研发工程师)

欢迎加入 **LiteCodeTool** 的二次开发！本项目遵循**极简实用主义、自包含自宿主、三层物理隔离**的设计原则，坚决杜绝过度治理。

本手册旨在帮助研发工程师在 **5 分钟内**理清架构脉络，极速上手新增组件、扩展数据字典、编写轻量测试与执行打包构建。

---

## 一、开发环境与开箱即用

根据开发机网络环境，选择对应的启动方式：

### 1. 离线/内网开发机 (推荐使用“全离线全功能版”)
- **开箱即用**：本软件包已内置 Windows `bin/node.exe` 与完整的 `source/liteCodeTool/node_modules/`；
- **免装依赖**：无需连接外网，无需执行 `npm install`；
- **启动开发服务器**：
  ```bash
  cd source/liteCodeTool
  npm run dev:demo
  ```
  终端会同时启动 Vite 前端（`http://localhost:5173`）与本地推流服务（`8787`），支持修改代码**页面秒级热更新 (HMR)**。

### 2. 有网络连接的开发机
```bash
cd source/liteCodeTool
npm install
npm run dev:demo
```

### 3. 极速离线编译轨 (甚至无需 node_modules)
若只需要修改纯逻辑或组件并发布到运行版：
```bash
node tools/build.mjs
```
该命令调用项目自带的 `vendor/typescript` 编译器，零 npm 依赖将 `src/` 离线打包输出至自包含应用目录。

---

## 二、代码架构全景与目录职责

```text
source/liteCodeTool/
├── src/                          # 前端 Vue 3 + TypeScript 源代码
│   ├── App.vue                   # 根组件：顶部工具栏、左侧资产库、中间画布、右侧属性检查器
│   ├── components/               # 核心大屏交互组件
│   │   ├── InstanceCard.vue      # 卡片容器：标题/副标题就地编辑、拖拽手柄、等比缩放把手、换船弹窗
│   │   ├── Inspector.vue         # 右侧属性检查器：置顶卡头设置、数据绑定、外观排版
│   │   ├── ControlRenderer.vue   # 原子控件渲染分发器：数值、文本、指示灯、表格、折线图等
│   │   ├── LineChart.vue         # 矢量 SVG 动态时序滑动折线图（槽位曲线与全局曲线）
│   │   └── ObjectDrawer.vue      # 显示态右侧滑出式全局监控对象指派抽屉
│   ├── engine/
│   │   └── core.ts               # 核心纯函数逻辑层：实体池合并、时效判定、字段求值、大屏契约校验
│   ├── runtime.ts                # 全局响应式状态机：数据推流解析、快照撤销/重做 (Undo/Redo)
│   ├── types.ts                  # 统一领域模型与 TypeScript 契约定义
│   ├── styles.css                # 科技蓝主题全量样式系统
│   └── main.ts                   # 前端应用挂载入口
│
├── server/                       # Node.js 本地持久化与 WebSocket 服务
│   ├── index.mjs                 # 主服务：HTTP 静态托管、REST API（导入导出保存）、WebSocket 推流
│   ├── dev.mjs                   # 演示模式虚拟推流模拟器（8艘模拟船舶航行状态）
│   └── defaults.mjs              # 初始大屏模板、内建数据字典与底账数据
│
├── tests/                        # 单元测试
│   └── core.test.ts              # 核心纯函数算法单元测试集（33 项测试）
│
├── tools/                        # 工具链
│   ├── launcher.mjs              # 零 npm 依赖的标准 Node.js 启动器
│   └── build.mjs                 # 独立离线单文件编译器
│
├── vendor/                       # 内置自宿主运行时（TypeScript 5.8 编译器、Vue 3 运行时、ws 库）
├── package.json                  # 项目依赖与开发脚本
└── vite.config.ts                # Vite 生产与开发配置
```

---

## 三、常用研发命令速查表

| 命令 | 描述 | 说明 |
| :--- | :--- | :--- |
| `npm run dev:demo` | 启动开发服务（带演示数据） | 推荐日常组件与交互开发，带 Vite HMR 热更新 |
| `npm run dev:live` | 启动开发服务（真实模式） | 面向真实外部接口推流调试，未收到数据字段显示 `--` |
| `npm test` | 执行全量单元测试 | 基于 Node.js 原生测试引擎，毫秒级快速回归，零外部依赖 |
| `npm run typecheck` | 执行 TypeScript 严格类型检查 | 执行 `vue-tsc --noEmit`，确保无任何类型隐患 |
| `npm run build` | 离线自包含编译 | 将 `src/` 离线编译打包输出至生产运行资产 |
| `npm run build:vite` | Vite 生产标准打包 | 标准 Tree-shaking 静态打包输出至 `../liteCodeTool_tmp/dist/` |

---

## 四、实战教程 1：如何新增一个大屏原子控件 (Control)

假设我们要新增一个【进度条 (progress)】原子控件：

### 步骤 1：在 `src/types.ts` 扩展控件类型定义
在 `ControlType` 类型联合中增加新控件标识，并在 `ControlProps` 中扩充该控件的私有配置项：
```typescript
// 1. 注册控件类型标识
export type ControlType = 'text' | 'number' | 'time' | 'light' | 'table' | 'line' | 'image' | 'progress';

// 2. 在通用控件属性接口中扩充新控件专有属性
export interface ControlProps {
  // ...既有属性
  min?: number;         // 进度条最小值，默认 0
  max?: number;         // 进度条最大值，默认 100
  color?: string;       // 进度条高亮颜色
}
```

### 步骤 2：在 `src/components/ControlRenderer.vue` 编写渲染逻辑
在模板中增加分支：
```vue
<template>
  <!-- 其他控件分支... -->
  <div v-else-if="control.type === 'progress'" class="control-progress">
    <div class="progress-label">
      <span>{{ control.label }}</span>
      <span class="progress-num">{{ displayValue }}%</span>
    </div>
    <div class="progress-track">
      <div class="progress-bar" :style="{ width: percent + '%', backgroundColor: control.color || '#22d3ee' }"></div>
    </div>
  </div>
</template>
```

### 步骤 3：在 `src/components/Inspector.vue` 增加属性配置项
当选中该控件时，在右侧属性检查器显示特有配置（如最大最小值、高亮色）：
```vue
<div v-if="selectedControl.type === 'progress'" class="inspector-group">
  <label>最大值</label>
  <input type="number" v-model.number="selectedControl.max" />
  <label>进度条颜色</label>
  <input type="color" v-model="selectedControl.color" />
</div>
```

---

## 五、实战教程 2：如何扩展数据字典与外部数据推流

### 1. 数据结构理念 (EntityStore)
- 系统采用**单向数据流与实体数据池（`EntityStore`）**架构；
- 每个实体对象（例如船舶 MMSI `413900001`）维护独立的当前状态底账与历史滑动窗口时序列表；
- 收到增量推送时，只合并有值字段；过期未更新的字段会自动标记为陈旧态（`stale`），前端显示为灰显或提示。

### 2. 在 `server/defaults.mjs` 扩展字段定义
```javascript
export const schemas = {
  vessel: {
    name: '船舶监控指标',
    fields: [
      { key: 'speed', label: '航速', unit: 'kn', precision: 1, type: 'number' },
      { key: 'heading', label: '船向', unit: '°', precision: 0, type: 'number' },
      // 新增自定义字段:
      { key: 'batteryLevel', label: '电量', unit: '%', precision: 0, type: 'number' },
    ]
  }
};
```

### 3. 向平台推流真实数据 (HTTP Ingest)
启动真实模式后，外部系统可通过标准的 HTTP POST 发送数据包：
```bash
curl -X POST http://127.0.0.1:8788/api/ingest \
  -H "Authorization: Bearer <您的connection.json安全凭据>" \
  -H "Content-Type: application/json" \
  -d '{
    "entityId": "413900001",
    "schemaType": "vessel",
    "timestamp": 1788720000000,
    "metrics": {
      "speed": 14.8,
      "batteryLevel": 92
    }
  }'
```
平台接收到数据后，所有绑定了该实体槽位的卡片、数值、指示灯及折线图会自动毫秒级实时刷新。

---

## 六、轻量测试编写与质量守则 (TDD)

本项目坚决践行**“轻量实用，杜绝过度治理”**原则：
- **不引入成套重型测试体系**（无庞大的 Selenium、Cypress、UI E2E 门禁）；
- **重点保护核心算法**：数据合并、时效判定、字段求值、滑动窗口纯函数；
- **极速回归**：直接在 `tests/core.test.ts` 中使用 Node 原生 `node:test` 与 `node:assert` 编写测试：

```typescript
import test from 'node:test';
import assert from 'node:assert/strict';
import { resolveValue } from '../src/engine/core.ts';

test('新控件字段解析在无数据时如实返回 --', () => {
  const result = resolveValue(null, 'batteryLevel');
  assert.equal(result.text, '--');
  assert.equal(result.isMissing, true);
});
```

运行测试：
```bash
npm test
```
秒级跑完所有测试，保障重构无回归隐患。

---

## 七、架构红线与合规开发原则

为保持工程的健壮性与纯洁性，二次开发请严格遵守以下红线：

1. **三层物理隔离体系**：
   - **代码仓（`source/liteCodeTool`）**：只存放纯源码与配置文件，严禁在仓库根目录下写入临时日志或持久化数据；
   - **数据外部隔离（`liteCodeTool_datas/`）**：运行时用户产生的大屏设计、模板库与推流底账自动存放至外部同级数据目录，实现程序与数据彻底解耦；
   - **临时目录隔离（`liteCodeTool_tmp/`）**：测试构建缓存、打包物存放于此。
2. **纯函数与展示层分离**：
   - 复杂的数据解析、时效算法必须作为纯函数放在 `src/engine/core.ts` 中并补充单元测试；组件只负责展示渲染与用户事件捕获。
3. **语言规范**：
   - 日常交流、代码注释与 Git Commit 说明严格遵循简体中文。